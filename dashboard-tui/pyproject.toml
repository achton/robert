[project]
name = "dashboard-tui"
version = "0.1.0"
description = "Terminal dashboard for a running Roberta instance"
requires-python = ">=3.13"
dependencies = [
    "textual>=0.82",
    "httpx>=0.27",
]

[tool.ruff]
line-length = 79
target-version = "py313"
